import { Helmet } from "react-helmet"

export default function Reviews() {
    return (
        <div id="error-page" className="flex justify-center items-center h-screen flex-col">
            <Helmet>
                <title>VM | Отзывы</title>
            </Helmet>
        </div>
    );
}